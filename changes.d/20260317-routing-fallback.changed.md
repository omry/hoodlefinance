Improved quote routing so fallback behavior now goes through an explicit shared route engine, `isin` now uses that same routing/adapter framework instead of a separate source-dispatch path, `IDENTIFIER@?` now shows the classifier bucket plus planned quote route instead of only the initially deduced source, generic ticker routes now show `YAHOO` unless the symbol is in the Israeli-fund subset that can actually fall through to TradingView, `HOODLEFINANCE_ROUTES()` now exposes the routing table in Sheets, `tools/cli.js --routing-table` now prints the static quote routing table, and `tools/cli.js --trace <symbol>` now prints a live runtime trace for one symbol.
