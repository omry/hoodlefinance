Improved route introspection so `IDENTIFIER@?` now shows the planned quote route, including the classifier bucket. `HOODLEFINANCE_ROUTES()` now exposes the routing table in Sheets, `tools/cli.js --routing-table` prints the static routing table, and `tools/cli.js --trace <symbol>` prints a live runtime trace for one symbol. Generic tickers now show `YAHOO` unless they are in the Israeli-fund subset that can actually fall through to TradingView.
